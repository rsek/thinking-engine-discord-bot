// import { ActionRow } from "discord.js";

// export default function deleteByCustomId(components: ActionRow[], customId: string) {
//   components.forEach(row => {
//     if (row.components.some(item => item.customId === customId)) {
//       row.setComponents(...row.components.filter(item => item.customId !== customId));
//     }
//   });
//   return components;
// }