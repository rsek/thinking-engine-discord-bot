// import { Embed } from "discord.js";
// import WithRequired from "../../types/WithRequired.js";

// export default function removeFieldByName(embed: WithRequired<Embed, "fields">, fieldName: string) {
//   return embed.setFields(...embed.fields.filter(field => field.name !== fieldName));
// }